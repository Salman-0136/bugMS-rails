<div class="max-w-xl mx-auto mt-10 p-8 bg-white rounded-2xl shadow-lg text-gray-800">
  <div class="text-center mb-6">
    <%= image_tag "email-change.png", alt: "Email Changed", class: "mx-auto w-32 h-32 object-contain" %>
    <h1 class="text-2xl font-bold text-purple-700 mt-4">Hello <%= @email %>!</h1>
  </div>

  <% if @resource.try(:unconfirmed_email?) %>
    <p>Your email is being changed to <strong><%= @resource.unconfirmed_email %></strong>. Please confirm it if you initiated this change.</p>
  <% else %>
    <p>Your email has been successfully changed to <strong><%= @resource.email %></strong>.</p>
  <% end %>

  <p class="text-sm text-gray-500 mt-4">If you did not request this, please contact support immediately.</p>
</div>
