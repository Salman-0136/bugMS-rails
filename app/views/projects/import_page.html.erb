<div class="max-w-3xl mx-auto mt-10 bg-white shadow-xl rounded-2xl p-10 border border-gray-100">

  <!-- Header -->
  <div class="flex items-center justify-between mb-8">
    <h2 class="text-4xl font-extrabold text-gray-900 tracking-tight">
      📄 Import Projects CSV
    </h2>

    <%= link_to projects_path, class: "text-blue-600 hover:text-blue-800 text-sm font-semibold transition" do %>
      ← Back to Projects
    <% end %>
  </div>

  <!-- Info Box -->
  <div class="p-5 mb-8 rounded-xl bg-blue-50 border border-blue-200">
    <p class="text-blue-800 font-medium flex items-center space-x-2">
      <span class="text-xl">💡</span>
      <span>Upload a CSV file containing projects. Ensure your column headers match the required fields.</span>
    </p>
  </div>

  <!-- Form -->
  <%= form_with url: import_projects_path, multipart: true, data: { turbo: false } do |f| %>
    <!-- Upload Box -->
    <label class="block">
      <span class="text-gray-700 font-semibold">Select CSV File</span>

      <div class="mt-3 flex items-center justify-center w-full">
        <label class="w-full flex flex-col items-center px-6 py-10 bg-gray-50 text-gray-700 rounded-xl shadow-inner border-2 border-dashed border-gray-300 cursor-pointer hover:bg-gray-100 transition">
          <span class="text-5xl">📁</span>
          <span class="mt-3 font-medium">Click to choose a CSV file</span>
          <%= file_field_tag :file, accept: ".csv", class: "hidden" %>

        </label>
      </div>
    </label>

    <!-- Submit Button -->
    <div class="mt-8">
      <%= f.submit "📥 Import CSV", 
            class: "bg-green-600 hover:bg-green-700 text-white px-8 py-3 rounded-xl font-semibold shadow-lg transition w-full text-center" %>
    </div>

  <% end %>

</div>
