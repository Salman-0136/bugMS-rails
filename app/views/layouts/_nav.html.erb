<nav class="bg-white shadow-md sticky top-0 z-50">
  <div class="max-w-8xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">

      <!-- Logo -->
      <div class="flex items-center w-full space-x-6 justify-between">
        <div class="flex-shrink-0 flex items-center flex align-left">
          <%= link_to "BugMS", root_path, class: "text-2xl font-bold text-purple-600" %>
        </div>

        <!-- Desktop & Tablet Links -->
        <div class="hidden md:flex items-center ml-5 space-x-4">
          <%= link_to "Home", root_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
          <%= link_to "Bugs", bugs_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
          <%= link_to "Projects", projects_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
          <%= link_to "About", about_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
          <%= link_to "Contact", contact_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
        </div>

        <% if user_signed_in? %>
          <div class="ml-4 w-full">
            <%= render "shared/search_filter" %>
          </div>
        <% end %>

        <!-- Desktop & Tablet Profile Dropdown -->
        <div class="hidden md:flex items-center relative ml-4" data-controller="dropdown">
          <% if user_signed_in? %>
            <div class="flex items-center max-w-xs space-x-4">
              <span class="truncate font-medium text-gray-700"><%= current_user.name %></span>
              <button type="button" data-action="click->dropdown#toggle">
                <%= image_tag current_user.profile_image.attached? ? url_for(current_user.profile_image) : "default-user.png",
                    class: "w-10 h-10 rounded-full object-cover border-2 border-purple-600 cursor-pointer" %>
              </button>
            </div>

            <div class="hidden absolute right-0 top-0 mt-12 w-80 bg-white shadow-lg rounded-lg z-50 origin-top-right" data-dropdown-target="menu">
              <%= link_to "Profile", user_path(current_user), class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
              <% if current_user.is_admin? %>
                <%= link_to "All Users", users_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
                <%= link_to "Admin Dashboard", admin_dashboard_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
              <% end %>
              <%= link_to "My Bugs", my_bugs_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
              <%= link_to "My Projects", my_projects_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
              <%= button_to "Logout", destroy_user_session_path, method: :delete,
                  class: "block w-full text-left px-4 py-2 text-red-600 hover:bg-red-50 rounded bg-white border-0" %>
            </div>
          <% else %>
            <div class="flex space-x-4">
              <%= link_to "Login", new_user_session_path, class: "bg-white border border-purple-600 px-4 py-2 text-purple-600 rounded-lg hover:bg-purple-50 font-medium" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-purple-600 px-4 py-2 text-white rounded-lg hover:bg-purple-700 font-medium" %>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Mobile Hamburger Menu -->
      <div class="md:hidden flex items-center relative" data-controller="dropdown">
        <button class="focus:outline-none w-8 h-8 flex items-center justify-center" data-action="click->dropdown#toggle">
          <svg class="w-8 h-8 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>

        <!-- Mobile Dropdown Menu -->
        <div class="hidden absolute right-0 top-0 mt-12 w-screen max-w-md sm:max-w-lg bg-white shadow-lg rounded-lg z-50 origin-top-right" data-dropdown-target="menu">
          <% if user_signed_in? %>
            <!-- Profile Section -->
            <div class="flex flex-col items-center px-4 py-3 border-b border-gray-200">
              <button type="button">
                <%= image_tag current_user.profile_image.attached? ? url_for(current_user.profile_image) : "default-user.png",
                    class: "w-10 h-10 rounded-full object-cover border-2 border-purple-600 cursor-pointer" %>
              </button>
              <span class="text-gray-700 font-medium text-center break-words"><%= current_user.name %></span>
            </div>

            <%= link_to "Profile", user_path(current_user), class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <% if current_user.is_admin? %>
              <%= link_to "All Users", users_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
              <%= link_to "Admin Dashboard", admin_dashboard_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <% end %>
            <%= link_to "My Bugs", my_bugs_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "My Projects", my_projects_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete,
                class: "block w-full text-left px-4 py-2 text-red-600 hover:bg-red-50 rounded bg-white border-0" %>
            <hr class="my-1 border-gray-200">

            <!-- Nav Links -->
            <%= link_to "Home", root_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Bugs", bugs_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Projects", projects_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "About", about_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Contact", contact_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
          <% else %>
            <!-- Mobile Nav Links for guests -->
            <%= link_to "Login", new_user_session_path, class: "block px-4 py-2 text-purple-600 hover:bg-purple-50" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "block px-4 py-2 text-white bg-purple-600 hover:bg-purple-700 rounded-lg font-medium" %>

            <hr class="my-1 border-gray-200">

            <%= link_to "Home", root_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Bugs", bugs_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Projects", projects_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "About", about_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
            <%= link_to "Contact", contact_path, class: "block px-4 py-2 text-gray-700 hover:bg-purple-50" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
