<div class="max-w-xl mx-auto mt-10 p-8 bg-white rounded-2xl shadow-lg text-gray-800">
  <div class="text-center mb-6">
    <%= image_tag "reset-password.png", alt: "Reset Password", class: "mx-auto w-32 h-32 object-contain" %>
    <h1 class="text-2xl font-bold text-purple-700 mt-4">Hello <%= @resource.email %>!</h1>
  </div>

  <p>Someone has requested a link to change your password.</p>

  <div class="text-center my-6">
    <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token),
          class: "bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-xl font-semibold shadow-md inline-block transition" %>
  </div>

  <p class="text-sm text-gray-500 mt-4">If you did not request this, ignore this email. Your password will not change.</p>
</div>
